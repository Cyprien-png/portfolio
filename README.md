# Portfolio

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 17.2.0.

## Build

Each push to the `main` branch trigger a GitHub Actions workflow that build the project in the `prod` branch.

## Deployment

The project is automatically deployed to my GitHub Pages by detecting changes on the root dir of `prod` branch.
